function Flights() {

    calculateNumberOfFlights(passengers, capacity) {
        let flights;

        if (passengers % capacity) {
            flights = (passengers / capacity) + 1;
        }
    }

}

